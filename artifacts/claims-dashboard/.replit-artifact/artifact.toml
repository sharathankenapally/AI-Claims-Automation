kind = "web"
previewPath = "/"
title = "Healthcare Claims Dashboard"
version = "1.0.0"
id = "artifacts/claims-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25068

[services.development]
run = "pnpm --filter @workspace/claims-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/claims-dashboard", "run", "build" ]
publicDir = "artifacts/claims-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25068"
BASE_PATH = "/"
